{context}

当前世界模板：{template_name}
模板提醒：{template_chunk_hint}

扫描以下文本，穷举其中所有世界观数据。

分块特有注意事项：
- world_info 在开头部分应完整填写；后续分段若直接出现作品/世界名称、时代背景、主线时间锚点等全局信息，也要填写可确认字段，用于补全前文缺失信息
- 同一人物的不同称呼要识别并用 aliases 记录
- 只提取**当前文本段**能直接确认的信息；该实体在别的章节的经历不要推测
- 任何在事件参与方、relationships.target、他人简介中被点名的实体都要单独输出
- 人物实体的"简介"应控制在约 1200 字以内，作为高密度概览；不要为了压缩简介而丢失 stages、实力变化、性格变化、关键转折，这些必须优先结构化输出
- 先在内部完成"识别人名与别名桥接 → 将经历归档到对应实体 → 再输出 JSON"，最终只输出 JSON

==== 文本开始 ====
{chunk}
==== 文本结束 ====

返回 JSON（结构遵循 System Prompt 定义）：
{{
  "world_info": {{"name": "", "description": "", "era": "", "anchor_time": ""}},
  "entities": [...],
  "events": [...],
  "settings": {{"items": [...], "mapData": {{...}}, "calendars": [...]}}
}}
