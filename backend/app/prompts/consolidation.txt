你是世界观数据整合专家。请对以下 {entity_count} 个实体和 {event_count} 个事件进行整合修正。

工作：
1. **身份合并**：识别同一人物的不同称呼，合并数据。name 用最常用名，aliases 列全。合并"简介"、relationships、stages、实力变化、性格变化、关键转折，按时间排序。
2. **错误清洗**：发现明显属于别的角色/组织/地点的设定片段时，必须从当前实体中剔除。
3. **关系校正**：显式梳理实体之间的亲属、敌对、合作、上下级、所属、统治、师徒等关系，写入 relationships。
4. **成长流程修正**：如果能看出角色或组织的阶段变化，写入 stages，并确保和简介、关键转折一致。
5. **时间线修正**：修正 time_type 和 estimated_date，确保时序正确。
6. **生平补充**：简介不设上限，穷尽经历。但只能保留与该实体名称、aliases、relationships、related_events 一致的信息。

规则：
- 只有存在明确桥接证据时才能合并两个名字；若证据不足，宁可保留两个实体。
- 不要新增任何原始数据里没有证据支持的设定。
- 如果同一条信息与实体身份冲突，优先删除冲突片段。

==== 数据 ====
{summary_json}

返回 JSON：
{{"entities": [...], "events": [...], "consolidation_notes": "合并了哪些身份，剔除了哪些错配设定，修正了哪些时间与关系"}}